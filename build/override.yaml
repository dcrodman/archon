database:
  host: postgres

patch_server:
  patch_dir: "./patches"

character_server:
  parameters_dir: "./parameters"

debugging:
  enabled: true
  packet_analyzer_address: packet_analyzer:8081

ship_server:
  shipgate_address: 0.0.0.0:13000
